commit 30759e0fa60ec0aa8bd0ece215122ed35dedcb55
Author: Backdoor Admin <admin@backdoor.co>
Date:   Mon Mar 23 03:26:11 2026 -0400

    Initialize Netlify Blobs in offer functions

diff --git a/netlify/functions/_shared/offers-store.js b/netlify/functions/_shared/offers-store.js
index 3f11d6f..8423de4 100644
--- a/netlify/functions/_shared/offers-store.js
+++ b/netlify/functions/_shared/offers-store.js
@@ -1,5 +1,5 @@
 const crypto = require('crypto');
-const { getStore } = require('@netlify/blobs');
+const { connectLambda, getStore } = require('@netlify/blobs');
 
 const FIREBASE_API_KEY = 'AIzaSyDq98ddvXGZLdxPCm0Gd-6gRtOmvBdBctw';
 const OFFER_STORE_NAME = 'customer-offers';
@@ -28,6 +28,11 @@ function getOffersStore() {
     return getStore(OFFER_STORE_NAME);
 }
 
+function initOffersStore(event) {
+    connectLambda(event);
+    return getOffersStore();
+}
+
 function keyForOffer(offerId) {
     return `offers/${offerId}.json`;
 }
@@ -58,7 +63,7 @@ async function verifyAdminRequest(event) {
     return payload.users[0];
 }
 
-async function createOffer(payload) {
+async function createOffer(event, payload) {
     const offerId = crypto.randomUUID();
     const timestamp = new Date().toISOString();
     const offer = {
@@ -81,12 +86,12 @@ async function createOffer(payload) {
         updatedAt: timestamp
     };
 
-    await getOffersStore().setJSON(keyForOffer(offerId), offer);
+    await initOffersStore(event).setJSON(keyForOffer(offerId), offer);
     return offer;
 }
 
-async function listOffers() {
-    const store = getOffersStore();
+async function listOffers(event) {
+    const store = initOffersStore(event);
     const { blobs } = await store.list({ prefix: 'offers/' });
     const offers = await Promise.all(blobs.map(async (blob) => {
         const offer = await store.get(blob.key, { type: 'json' });
@@ -98,13 +103,13 @@ async function listOffers() {
         .sort((left, right) => String(right.createdAt || '').localeCompare(String(left.createdAt || '')));
 }
 
-async function getOffer(offerId) {
-    return getOffersStore().get(keyForOffer(offerId), { type: 'json' });
+async function getOffer(event, offerId) {
+    return initOffersStore(event).get(keyForOffer(offerId), { type: 'json' });
 }
 
-async function updateOffer(offerId, patch) {
-    const store = getOffersStore();
-    const existing = await getOffer(offerId);
+async function updateOffer(event, offerId, patch) {
+    const store = initOffersStore(event);
+    const existing = await getOffer(event, offerId);
     if (!existing) {
         throw new Error('Offer not found.');
     }
@@ -120,8 +125,8 @@ async function updateOffer(offerId, patch) {
     return next;
 }
 
-async function deleteOffer(offerId) {
-    await getOffersStore().delete(keyForOffer(offerId));
+async function deleteOffer(event, offerId) {
+    await initOffersStore(event).delete(keyForOffer(offerId));
 }
 
 module.exports = {
diff --git a/netlify/functions/create-offer.js b/netlify/functions/create-offer.js
index 79e38be..70f1893 100644
--- a/netlify/functions/create-offer.js
+++ b/netlify/functions/create-offer.js
@@ -21,7 +21,7 @@ exports.handler = async (event) => {
             return json(400, { error: 'Missing required offer details.' });
         }
 
-        const offer = await createOffer(payload);
+        const offer = await createOffer(event, payload);
         return json(200, {
             ok: true,
             id: offer.id
diff --git a/netlify/functions/delete-offer.js b/netlify/functions/delete-offer.js
index bdd8aef..d88d055 100644
--- a/netlify/functions/delete-offer.js
+++ b/netlify/functions/delete-offer.js
@@ -19,7 +19,7 @@ exports.handler = async (event) => {
             return json(400, { error: 'Missing offer id.' });
         }
 
-        await deleteOffer(offerId);
+        await deleteOffer(event, offerId);
         return json(200, { ok: true });
     } catch (error) {
         const unauthorized = /unauthorized|missing admin token/i.test(String(error?.message || ''));
diff --git a/netlify/functions/list-offers.js b/netlify/functions/list-offers.js
index cc506a9..947298f 100644
--- a/netlify/functions/list-offers.js
+++ b/netlify/functions/list-offers.js
@@ -11,7 +11,7 @@ exports.handler = async (event) => {
 
     try {
         await verifyAdminRequest(event);
-        const offers = await listOffers();
+        const offers = await listOffers(event);
         return json(200, { offers });
     } catch (error) {
         const unauthorized = /unauthorized|missing admin token/i.test(String(error?.message || ''));
diff --git a/netlify/functions/update-offer.js b/netlify/functions/update-offer.js
index d80594d..2ad68f9 100644
--- a/netlify/functions/update-offer.js
+++ b/netlify/functions/update-offer.js
@@ -35,7 +35,7 @@ exports.handler = async (event) => {
             patch.counterAmount = counterAmount;
         }
 
-        const offer = await updateOffer(offerId, patch);
+        const offer = await updateOffer(event, offerId, patch);
         return json(200, { ok: true, offer });
     } catch (error) {
         const unauthorized = /unauthorized|missing admin token/i.test(String(error?.message || ''));
